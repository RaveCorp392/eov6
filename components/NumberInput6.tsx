'use client';import {useState} from 'react';export default function NumberInput6({onSubmit,length=6}:{onSubmit:(c:string)=>void,length?:number}){const[v,setV]=useState('');return(<form onSubmit={e=>{e.preventDefault();if(v.length===length)onSubmit(v)}} className='flex items-center gap-2'><input inputMode='numeric' pattern='[0-9]*' maxLength={length} value={v} onChange={e=>setV(e.target.value.replace(/\D/g,''))} className='w-48 rounded-xl border px-4 py-3 text-center tracking-[0.4em]' placeholder='••••••'/><button className='rounded-xl bg-blue-700 px-4 py-3 text-white disabled:opacity-50' disabled={v.length!==length}>Join secure chat</button></form>)}
